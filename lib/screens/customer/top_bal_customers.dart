import 'package:flutter/material.dart';

class TopBalCustomer extends StatelessWidget {
  const TopBalCustomer({super.key});
  static const routeName = 'topbal_customer';

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Top Balance Cusotmer'),
    );
  }
}
