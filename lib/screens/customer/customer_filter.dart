import 'package:flutter/material.dart';

class CustomerFilter extends StatelessWidget {
  const CustomerFilter({super.key});
  static const routeName = 'customer_filter';

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Customer Filter Page'),
    );
  }
}
